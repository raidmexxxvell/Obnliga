# Obnliga Constitution

<!-- Футбольная лига Обнинска - Telegram WebApp -->

Используется только русский язык для всех документов и комментариев к коду. Английский — только для кода и технических терминов.

## Основные Принципы

### I. Приоритет MCP Context7 (уточнённое поведение)

Агент **начинает** поиск артефактов с MCP Context7, но выполняет это по предопределённой стратегии поиска и строгой отчётности. Агент принимает решение автономно — не должен предлагать варианты пользователю в процессе работы; при низкой релевантности он автоматически выполняет заранее определённое действие (см. пункт 4). Если проектная политика явно разрешает запрос уточнения, это поведение можно переопределить через конфиг.

1. **Шаг 1 — Context7 (семантический поиск)**: выполнить семантический поиск в mcp Context7; вернуть топ-5 совпадений с оценками сходства (score).

   * Если `top_score ≥ 0.75` — считать артефакт релевантным и использовать/предложить адаптацию.
2. **Шаг 2 — Расширенный поиск по репозиторию**: если `top_score < 0.75`, выполнить поиск по локальному репозиторию (имена функций, TODO, интерфейсы), по shared libs и по директориям `src/`, `docs/`.
3. **Шаг 3 — Расширение запроса**: автоматическое расширение (синонимы, fuzzy search, альтернативные названия) и повторный поиск.
4. **Шаг 4 — Автодействие (фолбэк, без запросов пользователю)**: если после шагов 1–3 релевантность остаётся низкой (`top_score < 0.75`), агент **автоматически** выполняет заранее определённое действие и документирует его в `audit/mcp-context7-summary.md`. По умолчанию это действие — **создать `temporary stub`**, полностью помеченный и снабжённый планом синхронизации и списком TODO. Альтернатива (например, создание кода «с нуля») возможна только если roadmap явно помечает фичу как «создавать с нуля», или установлен флаг конфигурации `allow_manual_prompt = true`.
5. **Отчётность**: перед началом разработки агент генерирует `audit/mcp-context7-summary.md`, который содержит: точные поисковые запросы, список просканированных источников, топ-совпадения с оценками (score), причины отклонения и детальный план синхронизации для `temporary stub` (если создан).
6. **Исключения**: проектные правила/roadmap могут явно пометить фичи как «создавать с нуля» — в этом случае агент пропускает шаги 1–3.
7. **Порог для temporary stub**: пометка `temporary stub` ставится только при выполнении шагов 1–3 и при наличии подробного плана синхронизации; нельзя ставить stub без отчёта о выполненных поисках и причинах.

---

### II. Многоуровневое кэширование

Архитектура основана на multilevel cache (in-memory LRU + Redis) с обязательной поддержкой ETag + SWR (stale-while-revalidate).

* Каждый API endpoint должен поддерживать кэширование согласно `docs/cache.md`.
* TTL настраивается по категориям: league (5–30 сек), match details (10 мин), predictions (2–5 мин).
* Обязательна защита от cache-bomb с лимитом **50 записей на пользователя**.
* Кэш-инвалидация через Redis pub/sub для согласованности данных.

---

### III. Patch-based WebSocket (обязательно)

Все real-time обновления используют patch-based протокол (`patch|full`) с поддержкой topics, retry и graceful reconnect.

* Клиент обязан уметь обрабатывать как патчи, так и полные обновления.
* Для экономии ресурсов сервера следует: мультиплексировать каналы, агрегировать патчи, ставить soft-throttling на частые обновления и использовать компактную сериализацию (например, JSON diff или MessagePack).
* При ограничениях инфраструктуры (ограниченные соединения/фризы) предусмотреть fallback — polling с адаптивным интервалом и ETag.

---

### IV. Строгая типизация TypeScript

Все новые модули стора и API — только `.ts/.tsx`.

* Запрещены обходы типизации `(any)` без явного обоснования.
* Shared types синхронизируются между backend/frontend через `shared/types.ts`.
* При изменениях схемы БД — обязательная генерация Prisma Client (`npx prisma generate`).

---

### V. Визуальная система «Неокубизм»

UI следует концепции неокубизма с неоновыми акцентами согласно `docs/style.md`.

* Обязательные CSS-переменные: `--bg-1/--bg-2`, `--neon-cyan/--neon-magenta`, `--accent`.
* Все компоненты должны соответствовать WCAG AA для контрастности.
* Анимации ≤ 700ms, easing — cubic-bezier по дизайну.

---

## Архитектурные ограничения

### Технологический стек (фиксированный)

**Backend**: Node.js (≥18) + TypeScript + Prisma + Fastify + Redis + BullMQ (BullMQ — опционально, только при наличии выделенных воркеров).
**Frontend**: TypeScript + Vite + React/Preact + single-store (Zustand фасад).
**Deployment**: GitHub → Render.com (Managed Postgres + Redis).
**Database**: Полное соответствие схеме из `docs/BD.md` обязательно.

> Примечание по бесплатным инстансам: учитывая, что многие платные фоновые сервисы и serverless cron тоже платные, документ ориентирован на стратегии, работающие на бесплатных/ограниченных инстансах (см. раздел «Оптимизация под бюджет»).

---

## Производительность и стабильность

* Все API endpoints поддерживают ETag для экономии трафика.
* Обязательна метрика влияния на Retention/Engagement/Revenue/Tech Stability (🔴/🔵/⚪).
* Кэш-инвалидация через Redis pub/sub для согласованности данных.
* Real-time подключения с автоматическим переподключением и backoff strategy.

---

## Рабочий процесс

### Обязательные проверки (до любого изменения)

1. Анализ документации: `docs/project.md`, `docs/roadmap.md`, `docs/state.md`, `docs/style.md`, `docs/cache.md`, audit/changes/.
2. Статус в roadmap: проверка выполнения предварительных пунктов.
3. MCP Context7: анализ и создание `audit/mcp-context7-summary.md`.
4. Компоненты: перечисление затронутых модулей и влияния на сеть/кэш/WS.
5. Метрики: оценка влияния на ключевые показатели.

**Важно:** после внесения изменений в ту папку, где были модификации, запустить сборку: `npm run build` в соответствующей папке.
Если были изменения в схеме БД — выполнить `npx prisma generate`.

---

### Атомарность изменений

Один PR = одна цель.

* Изменения должны быть инкрементными и безопасными для продакшена.
* Обязательно указание «что было → что стало» с обоснованием безопасности коротко и по факту.
* Все изменения фиксируются в `audit/changes/<ID>.md`.

---

### Документация (обязательно)

Каждый крупный PR обновляет:

* `audit/mcp-context7-summary.md` (перед началом)
* `audit/changes/<PR_ID>.md` (запись изменений)
* `docs/state.md` (при изменениях стора)
* `docs/project.md` (структурные изменения)
* `docs/roadmap.md` (обновление статусов ✅/🟨/⬜)
* `docs/cache.md` (изменения в кэшировании)
* `docs/style.md` (изменения UI/UX)
* `docs/BD.md` (изменения в БД)

---

### Строгие запреты

1. Изменения до анализа MCP Context7 (если только изменение явно помечено как «создавать с нуля» в roadmap).
2. Production-код «с нуля» без попытки переиспользования.
3. Изменение бизнес-логики без согласования с roadmap.
4. Отключение ETag/WS/кэш без эквивалентной замены.
5. Vanilla JS для новых store-модулей (только TypeScript).
6. Игнорирование a11y требований.

---

## Оптимизация под ограниченные ресурсы (GitHub → Render.com, бесплатные Redis/DB, без платных background workers)

Поскольку Я использую бесплатные инстансы Redis/DB и избегаю платных фоновых воркеров, документ фокусируется на техниках, которые реально применимы на бесплатных тарифах и широко используются в профессиональных приложениях:

1. **Минимизировать количество долгоживущих соединений**

   * Мультиплексирование WebSocket (одно соединение на клиента, несколько topics внутри одного канала).
   * Для частей системы с высоким количеством клиентов предусмотреть переключение на polling с адаптивным интервалом (ETag + conditional GET) и экспоненциальным backoff.

2. **Экономное использование Redis**

   * LRU + строгая настройка TTL; лимиты по ключам и по объёму на пользователя.
   * Для pub/sub использовать компактные сообщения (диффы/пэчи), объединять события в batches и отправлять агрегированные обновления.
   * Хранить в Redis только критичные state-снэпшоты и diffs; избегать дублирования больших объёмов данных.

3. **Альтернативы фоновой обработке без платных воркеров**

   * Выполнять короткие фоновые задачи синхронно в HTTP-обработчике, если их время выполнения гарантированно мало.
   * Использовать лёгкие локальные worker-процессы (тогда они запускаются вместе с приложением, но нужно контролировать потребление памяти/CPU).
   * Для периодических задач — использовать бесплатные внешние cron-сервисы (например, cron-job.org или эквиваленты) либо GitHub Actions scheduled workflows (если политика репозитория позволяет). Обе опции могут иметь ограничения, но зачастую работают бесплатно для небольших нагрузок.

4. **Оптимизация WebSocket/реального времени**

   * Patch-based updates (малые диффы) + client-side optimistic updates ускоряют UX и снижают нагрузку.
   * Применять сжатие/минификацию сообщений, throttle и debounce на событиях, агрегацию частых изменений.

5. **Клиентская оптимизация**

   * Single-store (Zustand) + локальный SWR: отдавать stale данные и одновременно рефрешить фоновой перезагрузкой.
   * UI должен показывать прогресс / индикацию обновления вместо постоянных блокировок; применять optimistic UI для ключевых действий.

6. **Мониторинг лимитов и предохранители**

   * Собирайте метрики: количество подключений, memory usage, ops/sec для Redis/DB.
   * Внедрить автоматические предохранители: при приближении к лимитам — снижать частоту обновлений, переходить на polling, объединять события.

---

## Шаблон `audit/mcp-context7-summary.md` (обязательный)

(Агент заполняет автоматически при старте разработки фичи.)

```md
# audit/mcp-context7-summary
**Дата:** YYYY-MM-DD
**Запрос:** <короткое описание>
**Исполнитель:** <agent id/version>

## 1) Выполненные поисковые запросы
- context7 semantic query: "<запрос>" (params: model, top_k)
- repo fuzzy search: "<паттерны>"
- synonyms expansion: "<варианты>"

## 2) Источники просканированы
- Context7 index (last_indexed: YYYY-MM-DD) — N документов
- repo paths: /src/, /docs/, /shared/
- shared libs: <names>

## 3) Топ-5 совпадений (score)
- <file/path> — score 0.62 — <пояснение>
- ...

**Интерпретация:** top_score = <value> — <решение>

## 4) Выполненное действие (по умолчанию)
- Если top_score >= 0.75: адаптация найденного артефакта.
- Если top_score < 0.75: автоматически создан temporary stub с подробным планом синхронизации и списком TODO.

## 5) План синхронизации для temporary stub
- перечислить файлы для замены/адаптации
- list of interfaces/types to align
- тесты/контракты для проверки
- PR checklist

## 6) Следующие шаги (action items)
- [ ] Обновить roadmap, если требуется
- [ ] Создать PR с temporary stub и планом синхронизации
- [ ] При необходимости выполнить дополнительный внешний поиск и обновить audit
```

---

## Версия

**Версия:** 1.0.2 | **Утверждена:** 11 октября 2025 г. | **Последнее изменение:** 11 октября 2025 г.
